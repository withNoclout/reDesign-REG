# 📋 แหล่งรวมข้อมูล API ฝั่งมหาวิทยาลัย (reg2/reg3/regapiweb)

## 📌 ภาพรวมสถาปัตยกรรมใหม่
หลังจากมหาวิทยาลัยอัปเดตระบบ reg2 ปัจจุบันได้เปลี่ยนไปใช้ Angular SPA และมี REST API ที่คืนค่าเป็น JSON ทำให้เรา**ไม่ต้องทำ Web Scraping (HTML/Cheerio/VIEWSTATE) อีกต่อไป**

---

## ✅ 1. API ดึงรายชื่ออาจารย์ที่ค้างประเมิน (List Evaluations)
ทำงานได้สมบูรณ์แล้วในระบบปัจจุบัน

- **Endpoint**: `https://reg3.kmutnb.ac.th/regapiweb1/api/th/Evaluateofficer/Class`
- **Method**: `GET`
- **Auth**: Header `Authorization: Bearer <reg_token>`
- **Response**: Base64 + Gzip String -> แปลงแล้วได้ JSON Array

---

## ⚠️ 2. API ฝั่งแบบฟอร์มและการส่งผล (Evaluation Form & Submit)
เส้นทางอ้างอิงจาก Angular Bundle (ต้องใช้ `reg_token` แบบเดียวกับข้อ 1)

**Base URL ที่คาดว่าจะใช้**: `https://reg3.kmutnb.ac.th/regapiweb1/api/th` (หรือ `regapiweb3`)

### 2.1 ดึงโครงสร้างกลุ่มคำถาม (EvaluateGroup)
- **Endpoint**: `/Evaluateofficerform/Evaluategroup/{evaluateId}`
- **Method**: `GET`
- **Status**: ✅ ได้รับ HTTP 200 OK (ยืนยันแล้วจากการทดสอบก่อนหน้า)

### 2.2 ดึงข้อคำถาม (EvaluateQuestion)
- **Endpoint**: `/Evaluateofficerform/Evaluatequestion/{evaluateId}`
- **Method**: `GET`
- **Status**: 🔍 พบใน Bundle รอการยืนยันรูปแบบ Array ของ JSON

### 2.3 ส่งคำตอบ (AddAnswer)
- **Endpoint**: `/Evaluateofficerform/Addanswer/{...}` (อาจจะมี path param เพิ่มเติม)
- **Method**: `POST`
- **Payload Format**: `[...]` (JSON Array ของคำตอบ)
- **Status**: 🔍 พบใน Bundle ต้องใช้ Network Tab ดักข้อมูลจริงจาก Browser ตอนกดยืนยันในเว็บจริงเพื่อหา payload ที่ถูกต้อง

### 2.4 ยืนยันการส่งผลขั้นตอนสุดท้าย (Commit)
- **Endpoint**: `/Evaluateofficerform/commit/{...}`
- **Method**: `POST`
- **Status**: 🔍 พบใน Bundle มักจะเรียกหลังจาก Addanswer สำเร็จ

---

## 🔒 Session & Authentication
เราจะเปลี่ยนจาก **Session Cookie (`ASP.NET_SessionId`) + Web Scraping** ไปใช้ **JWT Token (`reg_token`) + REST API** ทั้งหมด เพื่อความลื่นไหลและความเสถียรของระบบ
